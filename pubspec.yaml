name: minerva_controller_generator
description: Minerva code generator for work with controllers.
version: 0.9.0
homepage: https://www.example.com

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  minerva_controller: ^0.9.0
  minerva_controller_annotation: ^0.9.0
  source_gen: ^1.2.6
  build: ^2.3.1
  analyzer: ^5.3.1
  minerva: ^0.3.0

dev_dependencies:
  lints: ^2.0.0
